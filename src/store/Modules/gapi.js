const namespaces = true;

const state = {
    authorized: false,
    sheets: [],
};

const getters = {};

const mutations = {};

const actions = {};

export default { namespaces, state, getters, mutations, actions };
